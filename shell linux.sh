# '' preserves all literals in shell but "" does not and also interpretes variable!

echo '"$HOME"'

# FOSS and FLOSS are two different philosopies of free and open/Libra software 
# Licenses are not all the same, the most strict is GPL since it does not allow sublicencing !

# cmds for sys admin works ... 
w # list logged users

last # last connected user

source # or . Execute a file for example or read conf e.g [@]$. .profile 

 